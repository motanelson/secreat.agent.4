game secreat agent in this mission 4 jim you must lock the .class file to be run be others and only run in the game virtual machine , like this only you can run the .class file , only by rename function main into init



openjdk-asmtools-jasm Hello.jasm -w .

sudo dnf install openjdk-asmtools.noarch

/usr/bin/openjdk-asmtools-jdis -w . Hello.class
